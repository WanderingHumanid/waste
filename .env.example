# ============================================================
# Waste Management Portal – Environment Variables
# Copy this file to .env.local and fill in your credentials.
# NEVER commit .env.local to version control.
# ============================================================

# ─── Supabase ───────────────────────────────────────────────
# Find these in: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ─── Groq AI (waste segregation chat) ───────────────────────
# Get a free key at: https://console.groq.com
GROQ_API_KEY=your-groq-api-key-here

# ─── Next.js ────────────────────────────────────────────────
# Must match your Supabase project's auth callback URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ─── Razorpay (fee payments – optional for dev) ─────────────
# Dashboard → Settings → API Keys
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your-razorpay-secret-here

# ─── Admin Seed Account ─────────────────────────────────────
# Used only by scripts/init-db.js to create the first admin user.
# Remove from production after initial setup.
ADMIN_SEED_EMAIL=admin@piravom.local
ADMIN_SEED_PASSWORD=change-me-immediately
